import { GameServiceState } from './GameServiceState';

export class GameServicePersistence extends GameServiceState {
    // Persistence-related helpers can be added here in future.
    // For now, behavior delegates to configured persistenceModule on GameServiceCore.
}
